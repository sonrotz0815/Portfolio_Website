---
const { title, description, tech, link, repo, img } = Astro.props;
---
<div class="p-5 rounded-3xl shadow-lg bg-white border border-purple-100 hover:shadow-2xl transition transform hover:-translate-y-1 hover:scale-105 fade-in text-center">
  {img && <img src={img} alt={title} class="mx-auto rounded-2xl mb-4 h-full max-h-40 object-cover shadow-inner" />}
  <h3 class="text-xl font-extrabold text-purple-700 mb-2">{title}</h3>
  <p class="text-gray-700 mb-2 leading-relaxed">{description}</p>
  <p class="text-sm text-purple-600 mb-4 font-medium">Technologien: {tech}</p>
  <div class="flex justify-center gap-3">
    {repo && <a href={repo} class="px-3 py-1 rounded-full bg-gradient-to-r from-purple-600 to-purple-900 text-white font-semibold shadow hover:shadow-lg hover:scale-105 transition-all duration-300">Code</a>}
  </div>
</div>